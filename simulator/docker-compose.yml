services:
    simulator:
        build: .
        image: whse
        volumes:
            - .:/work
        command: [
            "--dataDir", "./data/9",
            "--router", "./build/router-linux-amd64",
            "--info"
        ]
