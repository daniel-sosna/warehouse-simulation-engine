services:
    simulator:
        build: .
        image: whse-test
        volumes:
            - .:/app/output
        # These match your CMD in the Dockerfile
        command: ["--dataDir", "./data/6", "--router", "build/router/router-linux-amd64", "--eventLogFile", "
        ./output/results.log"]
